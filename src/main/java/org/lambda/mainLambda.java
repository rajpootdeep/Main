package org.lambda;

import java.util.function.Predicate;

public class mainLambda {
    public static  void main(String[] arg){
       /* Predicate<Integer> p= (n) -> n>10;
        Predicate<Integer> p1= (n) -> n<1100;
        final var and = p1.and(p);
        System.out.println(and.test(66));*/



    }
}
